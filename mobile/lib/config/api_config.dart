class ApiConfig {
  // Web / iOS simulator → localhost
  // Android emulator     → 10.0.2.2
  // Dispositivo físico   → IP de tu PC en la red local (ej. 192.168.1.x)
  static const String baseUrl = 'http://localhost:8080/api';
}
